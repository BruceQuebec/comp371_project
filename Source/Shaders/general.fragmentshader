#version 330 core

uniform vec3 customizedColoir = vec3(0.0f, 0.0f, 0.0f);

in vec3 fragmentColor;
out vec4 color;

void main()
{
	if(customizedColoir!=vec3(0.0f, 0.0f, 0.0f))
		color = vec4(customizedColoir, 1.0f);
	else
		color = vec4(fragmentColor, 1.0f);
}